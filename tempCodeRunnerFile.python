# Fast Tip :-
# --------------------------
# Whenever you deal with OpenAI models ➔
# Tokenizer first, encoding/decoding next!
# --------------------------

# made by Open AI Library(allows to encode and decode)
import tiktoken # type: ignore

# 1. Setup Encoder  // industry name : encoder
# im telling the ticktoken to use tokenizer that matches gpt 4o,and now my tokenizer is ready to encode or decode based on models vocab

my_encoder = tiktoken.encoding_for_model('gpt-4o')  

# just to check vocab size
print(my_encoder.n_vocab)  # 2. Know vocab size

tokens = my_encoder.encode("Hello bhidu!")  # 3. Text ➡️ Tokens

print("THE ENCODED TOKENS FROM TEXT : ")
# tokens now contain array of values
print(tokens) 

print("-----------------------------")

print("THE DECODED TEXT FROM TOKENS is :")
text = my_encoder.decode(tokens)  # 4. Tokens ➡️ Text
print(text)


# thoda sa khele iss token ke saath ??
print("KHELA SHURU KARTE HAI")
print("SABH KE DIN SHUBH HO :")
print("------------------------------")
print(tokens)

print("let's sort in ascending order")

tokens.sort()
print(tokens)

# now let's decode the tokens, unmeaningful answer we will get

mera_khel_wala_string=my_encoder.decode(tokens)

print("Unmeaningful things according to HUman brain will come :")

print("mera_khel_wala_string")

# lets see what each token is representing

# pehle token_2 ko list/array ki tarah bataana padega and in python that is defined as assigning values
token_2 = []


# python mein aise nahi kar saktein, since different hai iska internal working 

'''
for i in tokens:
    token_2[i] = tokens[i]
'''

''' Reason :
This is the problem. 🚨

token_2 is an empty list [].

Then you're doing token_2[i] = tokens[i].

But you can't assign to an index in a list that doesn't exist yet.

Lists must already have that many elements if you want to assign by index.

In Python, you can only assign to an index that already exists. But your token_2 is empty, and you are trying to write at positions like 13225, 11387, etc. (big numbers!). Of course — that index does not exist → IndexError.
'''

token_2 = tokens.copy()

# token_2[1] is a value and to put that separately in list, we need to pack that in [], hence [token_2[1]]

# Inspired from : 
# print([token_2[1]])
# print(token_2)

# Hum syntax mein gadbadi kar raha hai for loop ka , i is not the index but the element itself

j=0

print("------------------------------")
for i in token_2:
    print("At index :", j)
    # print([token_2[j]])
    temp_text_decoded = my_encoder.decode([token_2[j]])

    print(temp_text_decoded)
    
    print("length of each decoded text :", len(temp_text_decoded))
    j=j+1

print("------------------------------")












print("let's sort in descending order")

tokens.sort(reverse=True)
print(tokens)



